name: 'dbt_price'
version: '1.0.0'
config-version: 2
profile: 'price_intel'
model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"
models:
  dbt_price:
    +hours_to_expiration: 1440
    staging:
      +materialized: table
    cleaned:
      +materialized: table
    aggregated:
      +materialized: table
    marts:
      +materialized: table
